// Types for NavbarButton
export interface NavbarButtonProps {
  whiteBg: boolean;
}

// Types for BreadCrumb
export interface BreadCrumbProps {
  page: string;
  currentPage?: string;
}

export interface PageProp {
  page: string;
  currentPage: string;
  whiteBg: boolean;
}
